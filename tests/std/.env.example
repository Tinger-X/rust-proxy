# 代理服务器配置
# 此文件用于配置已运行的代理服务器的连接信息
# 用于测试标准的 HTTP/1.0, HTTP/1.1, HTTP/2, HTTPS, WebSocket 功能

# 代理服务器 IP 地址
PROXY_IP=127.0.0.1

# 代理服务器端口
PROXY_PORT=18000

# 代理认证用户名（如果不需要认证，留空即可）
PROXY_USERNAME=

# 代理认证密码（如果不需要认证，留空即可）
PROXY_PASSWORD=
